@*@model IEnumerable<AirCraftmodel>*@

@{
    ViewData["Title"] = "List of AirCraft Models";
}

<div class="row">
   @* @foreach (var item in Model)
    {*@
        <div class="col-md-4 col-xs-6 border-primary mb-3">
            <div class="card mb-3" style="max-width: 540px; ">
                <div class="row g-0">
                    <div class="col-md-12">
                        <div class="card-header text-white bg-dark">
                            <p class="card-text">
                                <h5 class="card-title" >
                                     PPC Sprint Air ATR-72-500 1/100
                                   
                                </h5>
                            </p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <img src="~/images/A1.jpg" width="100%" alt="@*@item.Name*@">
                    </div>
                    <div class="col-md-6">
                        <div class="card-body">
                            <p class="card-text">
                                Length: 20 cm

                                Material: Plastic

                                Brand: PPC
                            </p>
                            <p class="card-text"><b>SKU: </b>PP-SPRINT-ATR</p>
                            <p class="card-text"><b>WEIGHT: </b>1.00 KGS</p>
                            <p class="card-text"><b>SHIPPING </b>Calculated at Checkout</p>
                            <p class="card-text"><b>Price </b>Rs 4500000</p>
                            <p class="card-text ">
                                <b>Status: </b>
                               @* @if (DateTime.Now >= item.StartDate && DateTime.Now <= item.EndDate)
                                {*@
                                    <span class="badge bg-success text-white">AVAILABLE</span>
                               @* }
                                else if (DateTime.Now > item.EndDate)
                                {
                                    <span class="badge bg-danger text-white">Sold Out</span>
                                }
                                *@
                            </p>

                        </div>
                    </div>
                    <div class="col-md-12">
                        <div class="card-footer ">
                            <p class="card-text">
                                <a class="btn btn-outline-primary float-right" asp-action="Details" asp-route-id="@*@item.Id*@">
                                    <i class="bi bi-eye-fill"></i> Customize Order
                                </a>
                                <a class="btn btn-success text-white @*@((DateTime.Now > item.EndDate) ? "disabled" : "")"*@
                                   asp-controller="AirCraftmodel"
                                  @* asp-action="ShoppingCart"*@
                                   asp-route-id="@*@item.Id*@"
                                    onclick="addToCart('Aircraft Model', '4500000')">
                                    <i class="bi bi-cart-plus"></i> Add to Cart (Price 4500000 @*@item.Price.ToString("c")*@)
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
      @*2nd model *@
       
        <div class="col-md-4 col-xs-6 border-primary mb-3">
            <div class="card mb-3" style="max-width: 540px;">
                <div class="row g-0">
                    <div class="col-md-12">
                        <div class="card-header text-white bg-dark">
                            <p class="card-text">
                                <h5 class="card-title">
                                    @*@item.Name*@ PPC Sprint Air ATR-72-500 1/100
                                    @* @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                                {*@
                                    <a class="text-white float-right" asp-action="Edit" asp-route-id="@*@item.Id*@"><i class="bi bi-pencil-square"></i></a>
                                    @*}*@
                                </h5>
                            </p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <img src="~/images/A3.jpg" width="100%" alt="@*@item.Name*@">
                    </div>
                    <div class="col-md-6">
                        <div class="card-body">
                            <p class="card-text">
                                Length: 20 cm

                                Material: Plastic

                                Brand: PPC
                            </p>
                            <p class="card-text"><b>SKU: </b>PP-SPRINT-ATR</p>
                            <p class="card-text"><b>WEIGHT: </b>1.00 KGS</p>
                            <p class="card-text"><b>SHIPPING </b>Calculated at Checkout</p>
                            <p class="card-text"><b>Price </b>Rs 4500000</p>
                            <p class="card-text ">
                                <b>Status: </b>
                                @* @if (DateTime.Now >= item.StartDate && DateTime.Now <= item.EndDate)
                            {*@
                                @*<span class="badge bg-success text-white">AVAILABLE</span>*@
                               @*  }
                            else if (DateTime.Now > item.EndDate)
                            {*@
                                <span class="badge bg-danger text-white">Sold Out</span>
                          @*  }
                           *@
                            </p>

                        </div>
                    </div>
                    <div class="col-md-12">
                        <div class="card-footer ">
                            <p class="card-text">
                                <a class="btn btn-outline-primary float-right" asp-action="Details" asp-route-id="@*@item.Id*@">
                                    <i class="bi bi-eye-fill"></i> Customize Order
                                </a>
                                <a class="btn btn-success text-white @*@((DateTime.Now > item.EndDate) ? " disabled" : "" )"*@
                                  @* asp-controller="AirCraftmodel"
                                   asp-action="ShoppingCart"*@
                                   asp-route-id="@*@item.Id*@"
                                   onclick="addToCart('Aircraft Model', '4500000')">
                                    <i class="bi bi-cart-plus"></i> Add to Cart (Price 4500000 @*@item.Price.ToString("c")*@)
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div> 

        
   @* }*@
</div>
<script>
    function addToCart(itemName, itemPrice ) {
        // Your existing logic to add the item to the cart goes here

        // Show SweetAlert
        Swal.fire({
            title: 'Item Added to Cart',
            text: `${itemName} has been added to your cart.`,
            icon: 'success',
            confirmButtonText: 'OK',
            onClose: () => {
                // Redirect to the shopping cart page after the alert is closed
                window.location.href = '/AirCraftmodel/ShoppingCart';  // Adjust the URL as needed
            }
        });
    }
</script>
@*@await Html.PartialAsync("_CreateItem", "Movies") *@